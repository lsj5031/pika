# Ralph Progress Log - web-frontend-replace
Started: 2026-01-14
---

## [2026-01-14] - US-001: Initialize Vite + React + TypeScript project

**Implementation:**
- Created frontend-web/ directory using `npm create vite@latest -- --template react-ts`
- Installed base dependencies (React 19.2.0, TypeScript 5.9.3)
- Added required packages: @tanstack/react-query@5.90.16, zustand@5.0.10, lucide-react@0.562.0
- Verified dev server starts on port 5173
- Verified TypeScript compiles without errors (`npm run build`)

**Learnings:**
- Vite's project scaffolding is straightforward and creates proper TypeScript configuration out of the box
- The dev server starts quickly (137ms) - Vite is fast
- Vite uses project references in tsconfig (tsconfig.app.json, tsconfig.node.json)
- The .gitignore generated by Vite already excludes node_modules and dist

## [2026-01-14] - US-002: Configure Tailwind CSS and shadcn/ui

**Implementation:**
- Installed Tailwind CSS v4 using `@tailwindcss/postcss` package
- Created postcss.config.js with `@tailwindcss/postcss` plugin and autoprefixer
- Created tailwind.config.js for shadcn/ui theme customization (CSS variables for colors)
- Updated src/index.css with `@import "tailwindcss"` and shadcn/ui base styles (CSS custom properties for light/dark themes)
- Created components.json for shadcn/ui CLI configuration
- Installed class-variance-authority, clsx, tailwind-merge for utilities
- Created src/lib/utils.ts with cn() function for className merging
- Light theme is set as default via :root CSS variables
- Verified TypeScript compiles without errors

**Learnings:**
- Tailwind CSS v4 uses `@import "tailwindcss"` instead of `@tailwind` directives
- Tailwind v4 requires `@tailwindcss/postcss` plugin instead of the old `tailwindcss` PostCSS plugin
- The old v3 config file (tailwind.config.js) is still needed for shadcn/ui to customize theme
- Tailwind v4 doesn't support `@apply border-border` syntax - use plain CSS `border-color: hsl(var(--border))` instead

## [2026-01-14] - US-003: Create TypeScript types for API

**Implementation:**
- Created src/types/api.ts with all TypeScript types matching backend serde structs:
  - Project: id, path, name, session_count
  - Session: id, name, project_id, project_path, created_at, is_active
  - Message: role (user/assistant), content, timestamp
  - WSEvent: discriminated union with SessionStarted, SessionStopped, ThinkingDelta, MessageAdded
  - Request/response types for all API endpoints
- Created src/types/index.ts to export all types
- WSEvent uses TypeScript discriminated union matching Rust's serde "internally tagged" enum format
- Verified TypeScript compiles without errors

**Learnings:**
- Rust's `#[serde(tag = "type", content = "data")]` creates JSON with separate `type` and `data` fields
- TypeScript discriminated unions are the perfect match for Rust enums with serde tagging
- Backend API returns path fields as strings (not PathBuf objects) in JSON

## [2026-01-14] - US-018: Add environment variable configuration

**Implementation:**
- Created .env.example with VITE_API_URL and VITE_WS_URL documented
- Default values: VITE_API_URL=http://localhost:8080/api, VITE_WS_URL=ws://localhost:8080/ws
- Created src/config/env.ts for typed access to environment variables
- Used import.meta.env to access Vite env variables with fallback defaults
- Verified TypeScript compiles without errors

**Learnings:**
- Vite automatically loads .env files and makes VITE_ prefixed variables available via import.meta.env
- Environment variables are replaced at build time for static values
- The as const assertion on the config object provides better type inference

## [2026-01-14] - US-004: Create API client with React Query

**Implementation:**
- Created src/lib/api.ts with fetch-based apiClient using VITE_API_URL
  - Generic request<T> method with error handling
  - Convenience get<T> and post<T> methods
- Created React Query hooks:
  - hooks/useSessions.ts - fetches all sessions
  - hooks/useProjects.ts - fetches all projects
  - hooks/useSessionHistory.ts - fetches messages for a session
  - hooks/useCreateSession.ts - creates new session in project
  - hooks/useStartSession.ts - starts a session
  - hooks/useStopSession.ts - stops a running session
- All mutations include cache invalidation to keep data fresh
- Verified TypeScript compiles without errors

**Learnings:**
- React Query uses queryKey arrays for cache identification - nested arrays like ["sessions", sessionId, "messages"] work well
- Mutations should invalidate related queries to trigger refetching
- setQueryData can be used for optimistic updates after mutations

## [2026-01-14] - US-005: Create WebSocket hook

**Implementation:**
- Created hooks/useWebSocket.ts with the following features:
  - connectionStatus state: "connecting" | "connected" | "disconnected"
  - onMessage callback for handling WSEvent messages
  - enabled option to conditionally connect
  - Auto-reconnect on disconnect with exponential backoff (max 30s delay)
  - Proper cleanup on unmount (closes WebSocket with code 1000)
  - isConnected and isConnecting convenience booleans
- Uses VITE_WS_URL from config
- Verified TypeScript compiles without errors

**Learnings:**
- WebSocket cleanup requires both clearing the reconnect timeout and closing the connection
- Exponential backoff prevents overwhelming the server during outages
- Using refs for WebSocket and timeout ensures stable references across re-renders

## [2026-01-14] - US-011: Create Zustand store for global state

**Implementation:**
- Created store/appStore.ts using Zustand v5
- State includes:
  - currentSessionId: string | null
  - sidebarCollapsed: boolean
- Actions include:
  - setCurrentSession(sessionId) - sets the currently selected session
  - toggleSidebar() - toggles sidebar collapse state
  - setSidebarCollapsed(collapsed) - directly sets sidebar state
- Used zustand/middleware persist for localStorage persistence
- Storage key: "pi-agent-manager-storage"
- Verified TypeScript compiles without errors

**Learnings:**
- Zustand v5 uses a different API for middleware - wrap the store function with persist middleware
- The persist middleware automatically hydrates state from localStorage on mount
- Zustand stores don't require providers - can be used directly in any component

## [2026-01-14] - US-006: Create session list sidebar component

**Implementation:**
- Installed shadcn/ui ScrollArea component via `npx shadcn@latest add scroll-area`
- Created src/components/SessionList.tsx with:
  - Groups sessions by project using useSessions and useProjects hooks
  - Active status indicator (green dot) for active sessions
  - Click handler that updates Zustand store's currentSessionId
  - ScrollArea component for overflow handling
  - Loading state with "Loading..." message
  - Empty states for "No sessions" and "No projects found"
- Created src/components/index.ts for component exports
- Added QueryClientProvider to main.tsx for React Query support
- Updated App.tsx to display SessionList in sidebar layout
- Fixed scroll-area.tsx import to use relative path instead of @ alias (not configured yet)
- Verified TypeScript compiles without errors
- Verified in browser using browser-tools skill

**Learnings:**
- shadcn/ui CLI installs components directly into src/components/ui/
- The ScrollArea component uses @radix-ui/react-scroll-area under the hood
- Vite doesn't have @ alias configured by default - use relative imports or configure paths in vite.config.ts and tsconfig
- browser-tools skill requires Chrome to be started with remote debugging on port 9222 before navigating
- The component handles API errors gracefully by showing empty states when data is unavailable


## [2026-01-14] - US-007: Create session history view component

**Implementation:**
- Installed shadcn Card component via `npx shadcn@latest add card`
- Created src/components/SessionHistory.tsx with:
  - MessageBubble component for individual message rendering
  - User messages align right (justify-end), assistant messages align left (justify-start)
  - Card component styling with different colors for user (primary) vs assistant (muted)
  - Timestamp display formatted with localeTimeString below each message
  - Auto-scroll to bottom using messagesEndRef with smooth scroll behavior
  - Empty states: "Select a session to view history" when no session selected, "No messages yet" when no messages
  - Loading state shows "Loading messages..."
- Updated src/components/index.ts to export SessionHistory
- Updated App.tsx to use SessionHistory in main content area
- Fixed import path in card.tsx (changed "src/lib/utils" to relative path)
- Verified TypeScript compiles without errors
- Verified in browser using browser-tools skill

**Learnings:**
- The Card component uses React.forwardRef for proper ref forwarding
- Auto-scrolling to bottom on new messages is achieved with a ref (messagesEndRef) and scrollIntoView
- The useEffect dependency array with messages triggers scroll on each message update
- User messages use primary color, assistant messages use muted background for visual distinction
- Need to use relative imports (../../lib/utils) instead of @ alias which is not configured yet in Vite

## [2026-01-14] - US-008: Create chat input component

**Implementation:**
- Installed shadcn/ui Button and Textarea components via `npx shadcn@latest add button textarea`
- Created src/components/ChatInput.tsx with:
  - Textarea with auto-resize (max height 200px)
  - Send button with lucide-send icon
  - Keyboard handling: Enter to send, Shift+Enter for new line
  - Disabled state when no session selected, session inactive, or input empty
  - Dynamic placeholder text based on session state
  - Props: sessionId, isSessionActive, onSendMessage
- Created src/hooks/useSendPrompt.ts for API calls to POST /sessions/{id}/prompt
- Updated src/App.tsx:
  - Added useSessions hook to get session data
  - Added useSendPrompt hook for sending messages
  - Found current session and derived isSessionActive state
  - Added handleSendMessage callback with error handling
  - Integrated ChatInput component at bottom of main content area
  - Updated main layout to flex-col for proper ChatInput positioning
- Updated src/components/index.ts to export ChatInput
- Fixed import paths in button.tsx and textarea.tsx (changed "src/lib/utils" to relative paths)
- Verified TypeScript compiles without errors
- Verified in browser using browser-tools skill:
  - Textarea renders with "Select a session" placeholder when no session selected
  - Textarea is disabled when no session selected
  - Send button renders with lucide-send icon
  - Send button is disabled when no session selected
  - Proper flex layout with items-end gap-2 max-w-4xl mx-auto

**Learnings:**
- shadcn CLI generates components using "src/lib/utils" import path which doesn't work with Vite's default config
- Need to manually fix imports to use relative paths (../../lib/utils) for components
- Textarea auto-resize requires setting height to "auto" then to scrollHeight in a useEffect
- React Query mutations return a mutateAsync method for await-style error handling
- The isSessionActive state is derived from the session's is_active property from the API

## [2026-01-14] - US-015: Implement thinking state display

**Implementation:**
- Created src/store/thinkingStore.ts using Zustand v5 for per-session thinking state management:
  - thinkingBySession: Record<string, { content: string; isThinking: boolean }>
  - setThinking, appendThinking, clearThinking, getThinking actions
- Created src/components/ThinkingIndicator.tsx with:
  - Amber-colored Card component for visual distinction from regular messages
  - Animated Loader2 spinner from lucide-react
  - "Thinking" label with uppercase styling
  - Optional content display for streaming thinking content
- Updated src/components/SessionHistory.tsx:
  - Added useThinkingStore hook to get thinking state for current session
  - Conditionally render ThinkingIndicator when thinkingState.isThinking is true
  - Added thinkingState to useEffect dependency for auto-scroll
- Updated src/App.tsx:
  - Added useWebSocket hook with handleWebSocketMessage callback
  - Implemented WebSocket event handlers:
    - SessionStarted: invalidates sessions query
    - SessionStopped: clears thinking state and invalidates sessions
    - ThinkingDelta: appends content to thinking state
    - MessageAdded: clears thinking state and invalidates messages
  - Added useQueryClient hook for cache invalidation
- Updated src/components/index.ts to export ThinkingIndicator
- Verified TypeScript compiles without errors
- Verified in browser using browser-tools skill - app loads correctly with all components

**Learnings:**
- Zustand stores can be accessed with selectors to get specific state slices (e.g., (state) => state.thinkingBySession[sessionId])
- Using nullish coalescing (??) with empty state provides good default values for missing keys
- WebSocket event handlers should be wrapped in useCallback with proper dependencies
- The thinking state is per-session, allowing multiple sessions to have independent thinking states
- amber-50/amber-200 colors provide good visual distinction from user (primary) and assistant (muted) messages
- Using dark: variants ensures the component works in dark mode too
