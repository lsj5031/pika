# Ralph Progress Log - web-frontend-replace
Started: 2026-01-14
---

## [2026-01-14] - US-001: Initialize Vite + React + TypeScript project

**Implementation:**
- Created frontend-web/ directory using `npm create vite@latest -- --template react-ts`
- Installed base dependencies (React 19.2.0, TypeScript 5.9.3)
- Added required packages: @tanstack/react-query@5.90.16, zustand@5.0.10, lucide-react@0.562.0
- Verified dev server starts on port 5173
- Verified TypeScript compiles without errors (`npm run build`)

**Learnings:**
- Vite's project scaffolding is straightforward and creates proper TypeScript configuration out of the box
- The dev server starts quickly (137ms) - Vite is fast
- Vite uses project references in tsconfig (tsconfig.app.json, tsconfig.node.json)
- The .gitignore generated by Vite already excludes node_modules and dist

## [2026-01-14] - US-002: Configure Tailwind CSS and shadcn/ui

**Implementation:**
- Installed Tailwind CSS v4 using `@tailwindcss/postcss` package
- Created postcss.config.js with `@tailwindcss/postcss` plugin and autoprefixer
- Created tailwind.config.js for shadcn/ui theme customization (CSS variables for colors)
- Updated src/index.css with `@import "tailwindcss"` and shadcn/ui base styles (CSS custom properties for light/dark themes)
- Created components.json for shadcn/ui CLI configuration
- Installed class-variance-authority, clsx, tailwind-merge for utilities
- Created src/lib/utils.ts with cn() function for className merging
- Light theme is set as default via :root CSS variables
- Verified TypeScript compiles without errors

**Learnings:**
- Tailwind CSS v4 uses `@import "tailwindcss"` instead of `@tailwind` directives
- Tailwind v4 requires `@tailwindcss/postcss` plugin instead of the old `tailwindcss` PostCSS plugin
- The old v3 config file (tailwind.config.js) is still needed for shadcn/ui to customize theme
- Tailwind v4 doesn't support `@apply border-border` syntax - use plain CSS `border-color: hsl(var(--border))` instead

## [2026-01-14] - US-003: Create TypeScript types for API

**Implementation:**
- Created src/types/api.ts with all TypeScript types matching backend serde structs:
  - Project: id, path, name, session_count
  - Session: id, name, project_id, project_path, created_at, is_active
  - Message: role (user/assistant), content, timestamp
  - WSEvent: discriminated union with SessionStarted, SessionStopped, ThinkingDelta, MessageAdded
  - Request/response types for all API endpoints
- Created src/types/index.ts to export all types
- WSEvent uses TypeScript discriminated union matching Rust's serde "internally tagged" enum format
- Verified TypeScript compiles without errors

**Learnings:**
- Rust's `#[serde(tag = "type", content = "data")]` creates JSON with separate `type` and `data` fields
- TypeScript discriminated unions are the perfect match for Rust enums with serde tagging
- Backend API returns path fields as strings (not PathBuf objects) in JSON

